{
  "name": "jobflo-addons",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo for the Jobflo Addon SDK and the community addon registry.",
  "workspaces": [
    "packages/*",
    "addons/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present"
  },
  "license": "MIT",
  "author": "jsrothwell"
}
