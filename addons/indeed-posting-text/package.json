{
  "name": "@jobflo-addons/indeed-posting-text",
  "version": "1.0.0",
  "private": true,
  "description": "Parses the plain text of an Indeed job posting's detail panel (as copied straight off the page) into a normalized job.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist"
  },
  "author": "jsrothwell",
  "license": "MIT",
  "dependencies": {
    "@jobflo/addon-sdk": "*"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
